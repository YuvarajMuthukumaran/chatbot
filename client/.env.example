# Only needed for production builds (Vercel, Netlify, Render static site, etc).
# Leave unset for local dev — Vite's dev server proxies /api to the backend
# instead (see vite.config.js).
VITE_API_URL=https://your-backend.onrender.com
