# Committed on purpose: not a secret — this is the public backend URL,
# visible in any browser's network tab regardless. Vite loads this
# automatically during a production build, so Vercel needs no dashboard
# environment variable configured for this to take effect.
VITE_API_URL=https://chatbot-6lzw.onrender.com
